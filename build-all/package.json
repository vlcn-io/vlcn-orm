{
  "name": "@aphro/build-all",
  "version": "0.1.2",
  "main": "lib/index.js",
  "type": "module",
  "dependencies": {},
  "devDependencies": {
    "typescript": "^4.6.4"
  },
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc --build",
    "watch": "tsc --build -w",
    "deep-clean": "rm -rf ./lib || true && rm tsconfig.tsbuildinfo || true"
  },
  "jest": {
    "testMatch": [
      "**/__tests__/**/*.test.js"
    ]
  }
}
