/**
 * Add a `key` method to each expression?
 *  and just concat keys from plan to make cache key?
 *
 * How long and how much to hold in cache?
 *  size bounding?
 *  ref bounding?
 *  random eviction when hit size bounds?
 *
 * Should we be bound to a specific context too?
 */
class QueryCache {}
