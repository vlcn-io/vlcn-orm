// import { sql } from '@aphro/sql-ts';
// import initSqlJs from '@aphro/sql.js/dist/sql-wasm.js';
// import { Connection } from '../index.js';
// // import * as fs from 'fs';
// // const filebuffer = fs.readFileSync('./test.sqlite');

// initSqlJs().then(function (SQL) {
//   // const conn = new Connection(new SQL.Database());
//   // conn.query(sql`CREATE TABLE "foo" ( "id" bigint NOT NULL )`);
//   // conn.query(sql`INSERT INTO "foo" (${sql.value(1)})`);
//   // const result = conn.query(sql`SELECT * FROM "foo"`);
//   // console.log(result);
// });

test('see https://github.com/sql-js/sql.js/issues/517', () => {});
